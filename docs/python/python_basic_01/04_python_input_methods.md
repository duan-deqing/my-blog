# 04 输入函数：用户交互

## 基础用法：获取用户输入

核心要点总结：

- 基础特性：input(提示文字) 用于获取用户输入，返回值永远是字符串（str）。
- 类型转换：必须用 int()/float() 将数字字符串转为数值类型才能参与计算，转换失败时可用 try-except 捕获错误（如用户输入文字却要求转换为数字）。
- 多值输入：通过 split(分隔符) 分割字符串，实现一次输入多个数据（如姓名 + 年龄、多科成绩）。
- 特殊场景：密码输入需用 getpass.getpass() 隐藏显示，避免密码泄露。
